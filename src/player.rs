pub struct Player {
    pub name: String,
    pub health: i32,
    pub attack: i32,
    pub defense: i32,
    pub speed: i32,
    pub level: i32,
    pub experience: i32,
    pub experience_to_next_level: i32,
    // pub inventory: Vec<Item>,
    // pub equipped: Vec<Item>,
}
